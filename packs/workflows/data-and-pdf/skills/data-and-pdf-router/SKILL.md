---
name: data-and-pdf-router
description: Explain the compatibility bundle that combines document evidence extraction with structured-data analysis. Use only when one complete workflow genuinely needs both PDFs or papers and tables or datasets; document-only and data-only requests should load a focused pack instead.
---

# Data and PDF Router

Show that this compatibility choice installs `document-evidence` and `structured-data-analysis`. Confirm the combined scope in the visible installation plan and leave the operational procedures to those dependencies.

This router must never infer that a PDF alone needs data analysis or that a dataset alone needs document conversion.
